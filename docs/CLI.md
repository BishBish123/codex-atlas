# Atlas CLI Reference

> Auto-generated by `scripts/generate_cli_help.py`. Run `make cli-help` to refresh.

## Top-level

```
                                                                                
 Usage: atlas [OPTIONS] COMMAND [ARGS]...                                       
                                                                                
 Codex-Atlas: agentic GraphRAG over a codebase, exposed as an MCP server.       
                                                                                
╭─ Options ────────────────────────────────────────────────────────────────────╮
│ --debug          Print full tracebacks on error.                             │
│ --help           Show this message and exit.                                 │
╰──────────────────────────────────────────────────────────────────────────────╯
╭─ Commands ───────────────────────────────────────────────────────────────────╮
│ index      Walk `corpus`, parse every .py, embed every chunk, persist the    │
│            graph.                                                            │
│ ask        Run a single agent query end-to-end.                              │
│ search     One-shot retrieval — runs the router but skips the answer         │
│            synthesis.                                                        │
│ explain    One-shot agent run anchored on a qualified name.                  │
│ mcp        Start the Codex-Atlas MCP server.                                 │
│ eval       Run the golden test set and write a markdown + (optional) JSON    │
│            report.                                                           │
│ calibrate  Run Cohen's kappa calibration between the judge and human labels. │
╰──────────────────────────────────────────────────────────────────────────────╯
```

---

## `atlas index`

_Walk `corpus`, parse every .py, embed every chunk, persist the graph._

**Synopsis:** `atlas index [OPTIONS]`

```
                                                                                
 Usage: atlas index [OPTIONS] CORPUS                                            
                                                                                
 Walk `corpus`, parse every .py, embed every chunk, persist the graph.          
                                                                                
╭─ Arguments ──────────────────────────────────────────────────────────────────╮
│ *    corpus      PATH  Directory to index. [required]                        │
╰──────────────────────────────────────────────────────────────────────────────╯
╭─ Options ────────────────────────────────────────────────────────────────────╮
│ --graph-out            PATH     Where to persist the call graph.             │
│                                 [default: data/graph.json]                   │
│ --encoder              TEXT     `fake` (deterministic blake2b) or any        │
│                                 sentence-transformers model id.              │
│                                 [default: fake]                              │
│ --max-files            INTEGER  Cap files for fast smoke runs.               │
│ --drop-existing                 DROP TABLE before reinserting.               │
│ --format               TEXT     `rich` (default; pretty Console output) or   │
│                                 `json` (machine-readable index dump).        │
│                                 [default: rich]                              │
│ --skip-embed                    Skip pgvector upsert. Useful with --format   │
│                                 json for graph-only debug.                   │
│ --store                TEXT     `memory` (default; serialises to             │
│                                 data/chunks.json — no DB) or `postgres`      │
│                                 (pgvector via POSTGRES_DSN).                 │
│                                 [default: memory]                            │
│ --chunks-out           PATH     Where to persist the in-memory chunk         │
│                                 snapshot when --store=memory.                │
│                                 [default: data/chunks.json]                  │
│ --graph-backend        TEXT     Call-graph backend: `networkx` (default;     │
│                                 in-memory) or `neo4j` (async Neo4j driver;   │
│                                 reads ATLAS_NEO4J_URI, ATLAS_NEO4J_USERNAME, │
│                                 ATLAS_NEO4J_PASSWORD).                       │
│                                 [default: networkx]                          │
│ --help                          Show this message and exit.                  │
╰──────────────────────────────────────────────────────────────────────────────╯
```

---

## `atlas ask`

_Run a single agent query end-to-end._

**Synopsis:** `atlas ask [OPTIONS]`

```
                                                                                
 Usage: atlas ask [OPTIONS] QUESTION                                            
                                                                                
 Run a single agent query end-to-end.                                           
                                                                                
╭─ Arguments ──────────────────────────────────────────────────────────────────╮
│ *    question      TEXT  Free-form question to ask the agent. [required]     │
╰──────────────────────────────────────────────────────────────────────────────╯
╭─ Options ────────────────────────────────────────────────────────────────────╮
│ --graph          PATH     Persisted call graph. [default: data/graph.json]   │
│ --encoder        TEXT     Encoder used at index time. [default: fake]        │
│ --top-k          INTEGER  Top-k retrieval cap. [default: 8]                  │
│ --store          TEXT     `memory` (default; reads data/chunks.json — no DB) │
│                           or `postgres` (pgvector via POSTGRES_DSN).         │
│                           [default: memory]                                  │
│ --chunks         PATH     Path to the chunk snapshot when --store=memory.    │
│                           [default: data/chunks.json]                        │
│ --backend        TEXT     Agent backend: `hand-rolled` (default) or          │
│                           `langgraph` (requires `uv sync --extra real`).     │
│                           [default: hand-rolled]                             │
│ --help                    Show this message and exit.                        │
╰──────────────────────────────────────────────────────────────────────────────╯
```

---

## `atlas search`

_One-shot retrieval — runs the router but skips the answer synthesis._

**Synopsis:** `atlas search [OPTIONS]`

```
                                                                                
 Usage: atlas search [OPTIONS] QUERY                                            
                                                                                
 One-shot retrieval — runs the router but skips the answer synthesis.           
                                                                                
╭─ Arguments ──────────────────────────────────────────────────────────────────╮
│ *    query      TEXT  Free-form code search query. [required]                │
╰──────────────────────────────────────────────────────────────────────────────╯
╭─ Options ────────────────────────────────────────────────────────────────────╮
│ --graph          PATH     Persisted call graph. [default: data/graph.json]   │
│ --encoder        TEXT     Encoder used at index time. [default: fake]        │
│ --top-k          INTEGER  Top-k retrieval cap. [default: 8]                  │
│ --format         TEXT     `rich` (default) or `json` for one-shot            │
│                           machine-readable output.                           │
│                           [default: rich]                                    │
│ --store          TEXT     `memory` (default; reads data/chunks.json — no DB) │
│                           or `postgres` (pgvector via POSTGRES_DSN).         │
│                           [default: memory]                                  │
│ --chunks         PATH     Path to the chunk snapshot when --store=memory.    │
│                           [default: data/chunks.json]                        │
│ --help                    Show this message and exit.                        │
╰──────────────────────────────────────────────────────────────────────────────╯
```

---

## `atlas explain`

_One-shot agent run anchored on a qualified name._

**Synopsis:** `atlas explain [OPTIONS]`

```
                                                                                
 Usage: atlas explain [OPTIONS] QUALIFIED_NAME                                  
                                                                                
 One-shot agent run anchored on a qualified name.                               
                                                                                
 Forces ``route=structural`` (graph-walk) so the classifier never               
 rewrites a structural intent — passing a bare qualified name like              
 ``pkg.mod.Cls.method`` previously sometimes routed as ``lookup``               
 (vector-only) when the heuristic missed the "who calls" prefix.                
 The structural route walks the call graph directly, which is what              
 every ``explain`` caller actually wants.                                       
                                                                                
 Query: passed straight through as the bare ``qualified_name``. The             
 structural extractor (``_extract_qualified_name``) only needs the              
 qname token and ignores any prefix — the previous "who calls X"                
 rewrite was inherited from the era when the classifier had to see              
 the keyword phrase to pick the route. With ``route_override`` doing            
 that selection up-front, the rewrite is dead weight; dropping it               
 also produces a cleaner answer header (``# pkg.mod.fn`` vs                     
 ``# who calls pkg.mod.fn``) in the synth output.                               
                                                                                
╭─ Arguments ──────────────────────────────────────────────────────────────────╮
│ *    qualified_name      TEXT  Qualified name to explain. [required]         │
╰──────────────────────────────────────────────────────────────────────────────╯
╭─ Options ────────────────────────────────────────────────────────────────────╮
│ --graph          PATH  Persisted call graph. [default: data/graph.json]      │
│ --encoder        TEXT  Encoder used at index time. [default: fake]           │
│ --store          TEXT  `memory` (default; reads data/chunks.json — no DB) or │
│                        `postgres` (pgvector via POSTGRES_DSN).               │
│                        [default: memory]                                     │
│ --chunks         PATH  Path to the chunk snapshot when --store=memory.       │
│                        [default: data/chunks.json]                           │
│ --help                 Show this message and exit.                           │
╰──────────────────────────────────────────────────────────────────────────────╯
```

---

## `atlas mcp`

_Start the Codex-Atlas MCP server._

**Synopsis:** `atlas mcp [OPTIONS]`

```
                                                                                
 Usage: atlas mcp [OPTIONS]                                                     
                                                                                
 Start the Codex-Atlas MCP server.                                              
                                                                                
╭─ Options ────────────────────────────────────────────────────────────────────╮
│ --transport        TEXT     MCP transport: stdio | http [default: stdio]     │
│ --host             TEXT     HTTP bind host. [default: 127.0.0.1]             │
│ --port             INTEGER  HTTP bind port. [default: 8090]                  │
│ --store            TEXT     `memory` (default; reads data/chunks.json — no   │
│                             DB) or `postgres` (pgvector via POSTGRES_DSN).   │
│                             [default: memory]                                │
│ --chunks           PATH     Path to the chunk snapshot when --store=memory.  │
│                             [default: data/chunks.json]                      │
│ --help                      Show this message and exit.                      │
╰──────────────────────────────────────────────────────────────────────────────╯
```

---

## `atlas eval`

_Run the golden test set and write a markdown + (optional) JSON report._

**Synopsis:** `atlas eval [OPTIONS]`

```
                                                                                
 Usage: atlas eval [OPTIONS]                                                    
                                                                                
 Run the golden test set and write a markdown + (optional) JSON report.         
                                                                                
╭─ Options ────────────────────────────────────────────────────────────────────╮
│ --graph                 PATH   Persisted call graph.                         │
│                                [default: data/graph.json]                    │
│ --encoder               TEXT   Encoder used at index time. [default: fake]   │
│ --out                   PATH   Where to write the markdown report.           │
│                                [default: evals/REPORT.md]                    │
│ --json-out              PATH   Optional JSON dump of per-question scores.    │
│ --failure-report        PATH   Optional JSONL dump for per-question manual   │
│                                debugging.                                    │
│ --baseline              PATH   Path to baseline JSON (aggregate metrics).    │
│                                Exits non-zero on regression.                 │
│ --baseline-out          PATH   Write the aggregate metrics of this run to a  │
│                                baseline JSON file. Use to refresh            │
│                                `evals/baseline.json` after material code     │
│                                changes.                                      │
│ --tolerance             FLOAT  Fractional tolerance for the baseline         │
│                                regression gate.                              │
│                                [default: 0.05]                               │
│ --store                 TEXT   `memory` (default; dict-backed, rebuilds the  │
│                                index from the corpus on the fly — no DB      │
│                                required) or `postgres` (pgvector via         │
│                                POSTGRES_DSN).                                │
│                                [default: memory]                             │
│ --corpus                PATH   Source tree to index when --store=memory.     │
│                                [default: src]                                │
│ --rebuild-graph                Parse `--corpus` and write `--graph` from     │
│                                scratch before evaluating.                    │
│ --judge                 TEXT   Answer-faithfulness judge mode: `auto`        │
│                                (LLMJudge when env key present, else          │
│                                HeuristicJudge), `llm` (always LLM; raises if │
│                                no key), `heuristic` (always deterministic,   │
│                                no LLM).                                      │
│                                [default: auto]                               │
│ --metrics               TEXT   Comma-separated list of metric groups to      │
│                                compute. `custom` = existing harness metrics  │
│                                (route correctness, citations, faithfulness). │
│                                `ragas` = add RAGAS-style metrics             │
│                                (faithfulness, answer_relevancy,              │
│                                context_precision, context_recall). Default:  │
│                                `custom,ragas`. Use `custom` alone to skip    │
│                                RAGAS for speed.                              │
│                                [default: custom,ragas]                       │
│ --help                         Show this message and exit.                   │
╰──────────────────────────────────────────────────────────────────────────────╯
```

---

## `atlas calibrate`

_Run Cohen's kappa calibration between the judge and human labels._

**Synopsis:** `atlas calibrate [OPTIONS]`

```
                                                                                
 Usage: atlas calibrate [OPTIONS]                                               
                                                                                
 Run Cohen's kappa calibration between the judge and human labels.              
                                                                                
╭─ Options ────────────────────────────────────────────────────────────────────╮
│ --labels        PATH  Path to the human-labelled calibration CSV.            │
│                       [default: evals/calibration.csv]                       │
│ --out           PATH  Where to write the CALIBRATION.md report.              │
│                       [default: evals/CALIBRATION.md]                        │
│ --judge         TEXT  Judge mode: `auto`, `llm`, or `heuristic`.             │
│                       [default: auto]                                        │
│ --help                Show this message and exit.                            │
╰──────────────────────────────────────────────────────────────────────────────╯
```

---

## `atlas-mcp` (MCP server entry-point)

_Standalone entry-point that starts the Codex-Atlas MCP server directly (equivalent to `atlas mcp`)._

**Synopsis:** `atlas-mcp [OPTIONS]`

```
                                                                                
 Usage: atlas-mcp [OPTIONS]                                                     
                                                                                
 Run the Codex-Atlas MCP server.                                                
                                                                                
╭─ Options ────────────────────────────────────────────────────────────────────╮
│ --transport        TEXT     MCP transport: stdio | http [default: stdio]     │
│ --host             TEXT     HTTP bind host. [default: 127.0.0.1]             │
│ --port             INTEGER  HTTP bind port. [default: 8090]                  │
│ --help                      Show this message and exit.                      │
╰──────────────────────────────────────────────────────────────────────────────╯
                                                                                
 Environment variables (read at first-tool-call time):   ATLAS_STORE            
 memory (default; reads ATLAS_CHUNKS_PATH) | postgres   ATLAS_GRAPH_PATH        
 persisted call graph; default data/graph.json   ATLAS_CHUNKS_PATH   chunk      
 snapshot when ATLAS_STORE=memory; default data/chunks.json   ATLAS_ENCODER     
 fake (default; deterministic blake2b) | sentence-transformers model id         
 ATLAS_STEP_TIMEOUT_S per-step (retriever/synth) timeout; default 10, 0         
 disables   ATLAS_RUN_TIMEOUT_S  whole-run timeout; default 30, 0 disables      
 POSTGRES_DSN        required when ATLAS_STORE=postgres Use absolute paths      
 under MCP — clients launch the server with an unpredictable cwd.
```

---
